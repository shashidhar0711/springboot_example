package com.mercedesbenz.reifenlabel.controller;

import org.springframework.boot.test.context.SpringBootTest;
@SpringBootTest
class CodeConfigControllerTest {
	/*
	 * CodeConfigController codeConfigController = null;
	 * 
	 * @Mock private ReifenlabelService reifenLabelService;
	 * 
	 * @BeforeEach public void setup() { codeConfigController = new
	 * CodeConfigController(); ReflectionTestUtils.setField(codeConfigController,
	 * "reifenLabelService", reifenLabelService);
	 * Mockito.when(reifenLabelService.getEmployees("all")).thenReturn(getEmployees(
	 * "all")); }
	 * 
	 * @Test void testGetEmployees() { ResponseEntity<List<Employee>>
	 * listOfEmployees = codeConfigController.getEmployees("all"); if
	 * (listOfEmployees !=null && listOfEmployees.getBody()!=null ) { List<Employee>
	 * listEmp=listOfEmployees.getBody(); if(!listEmp.isEmpty()) {
	 * assertEquals("address0", listEmp.get(0).getAddress()); } } }
	 * 
	 * 
	 * public List<Employee> getEmployees(String type) { List<Employee> emplist =
	 * new LinkedList<>(); for (int i = 0; i <= 10; i++) { Employee emp = new
	 * Employee(); emp.setSalary(10000 + i); emp.setEmpid(9999 + i);
	 * emp.setAddress("address" + i); emplist.add(emp); } return emplist;
	 * 
	 * }
	 * 
	 */}
