package com.mercedesbenz.reifenlabel.dao;

import org.springframework.boot.test.context.SpringBootTest;
@SpringBootTest
class ReifenlabelDaoImplTest {
	/*
	 * @Autowired ReifenlabelDaoImpl reifenLabelDaoImpl;
	 * 
	 * @Test void testGetEmployees() { List<Employee> employeeList =
	 * reifenLabelDaoImpl.getEmployees("all"); if(employeeList !=null &&
	 * !employeeList.isEmpty()) {
	 * 
	 * assertEquals("address0", employeeList.get(0).getAddress()); }
	 * 
	 * }
	 * 
	 */}
