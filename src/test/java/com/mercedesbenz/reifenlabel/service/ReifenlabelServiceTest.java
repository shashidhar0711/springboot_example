package com.mercedesbenz.reifenlabel.service;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ReifenlabelServiceTest {
	/*
	 * 
	 * ReifenlabelService reifenLabelService = null;
	 * 
	 * @Mock private ReifenlabelDaoImpl reifenLabelDaoImpl;
	 * 
	 * @BeforeEach public void setup() { reifenLabelService = new
	 * ReifenlabelService(); ReflectionTestUtils.setField(reifenLabelService,
	 * "reifenLabelDaoImpl", reifenLabelDaoImpl);
	 * Mockito.when(reifenLabelDaoImpl.getEmployees("all")).thenReturn(getEmployees(
	 * "all")); }
	 * 
	 * @Test void testGetEmployees() { List<Employee> listOfEmployees =
	 * reifenLabelService.getEmployees("all"); if (listOfEmployees != null &&
	 * !listOfEmployees.isEmpty()) { assertEquals("address0",
	 * listOfEmployees.get(0).getAddress()); } }
	 * 
	 * public List<Employee> getEmployees(String type) { List<Employee> emplist =
	 * new LinkedList<>(); for (int i = 0; i <= 10; i++) { Employee emp = new
	 * Employee(); emp.setSalary(10000 + i); emp.setEmpid(9999 + i);
	 * emp.setAddress("address" + i); emplist.add(emp); }
	 * 
	 * return emplist;
	 * 
	 * }
	 * 
	 */}
